# Отчёт о готовности заданий

Проверены все папки заданий `task_1`-`task_6` (исключая `task_7`) только по содержимому репозитория, без изменений файлов и без доработок.

## Статус по заданиям

- `task_1` — **готово**
  - Есть оформленный отчёт: `task_1/otchet_issledovanie_rag_infrastruktury.md`.
  - Покрыты сравнения LLM, эмбеддингов, ChromaDB/FAISS, варианты инфраструктуры и итоговый выбор.

- `task_2` — **частично готово**
  - Есть база с 30+ документами: `task_2/final_fandom/` (33 `.txt` файла).
  - Есть словарь и скрипты преобразования: `task_2/kimetsu_articles/DICTIONARY.md`, `apply_dictionary.py`, `clean_edited.py`.
  - **Не найден требуемый файл** `terms_map.json` (в явном виде по заданию).

- `task_3` — **в целом готово**
  - Есть скрипт индексации: `task_3/build_index.py`.
  - Есть индекс ChromaDB: `task_3/chroma_db/`.
  - Есть пример поискового запроса: `task_3/query_example.py`.
  - Есть README с описанием модели/базы/запуска: `task_3/README.md`.
  - Замечание: в README метрики «сколько чанков/время» оставлены как «см. вывод скрипта», без зафиксированных значений.

- `task_4` — **готово**
  - Реализован RAG-пайплайн: `task_4/rag.py` (поиск + промпт + генерация).
  - Есть few-shot и CoT через шаблоны: `task_4/prompts/few_shot.txt`, `task_4/prompts/system.txt`, `task_4/prompts/main.txt`.
  - Есть запускаемый интерфейс: `task_4/bot.py`.
  - Есть примеры диалогов, включая «Я не знаю»: `task_4/DIALOGS.md`.

- `task_5` — **готово**
  - Есть «злонамеренный» документ: `task_5/malicious_doc.txt`.
  - Есть скрипт индексации этого документа: `task_5/index_malicious_doc.py`.
  - Есть отдельный индекс для эксперимента: `task_5/chroma_db/`.
  - Есть лог серии тестов с режимами `off/protected`: `task_5/LOG_EXECUTION.md` (присутствуют и успешные, и фильтрованные/отклонённые кейсы).

- `task_6` — **частично готово**
  - Есть скрипт автообновления: `task_6/update_index.py`.
  - Есть обёртка под планировщик: `task_6/update_index.sh`.
  - Есть архитектурная диаграмма: `task_6/architecture.puml`.
  - Есть описание и пример лога: `task_6/README.md`, `task_6/LOG_EXECUTION.md`.
  - **Не найден фактический лог выполнения** `task_6/logs/index_update.jsonl` в репозитории.
  - Настройка `cron` описана, но как установленная конфигурация в репозитории не зафиксирована (проверить по файлам невозможно).

## Итог

- **Готово:** `task_1`, `task_3`, `task_4`, `task_5`
- **Частично готово:** `task_2`, `task_6`
- **Критичные пробелы:** отсутствие `task_2/terms_map.json`, отсутствие фактического лога запуска для `task_6` в репозитории.
