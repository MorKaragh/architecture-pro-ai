Это пример документа для Задания 6.

Скрипт `update_index.py` должен:
1) разбить документ на чанки;
2) сгенерировать эмбеддинги (в `stub`-режиме — детерминированно);
3) добавить чанки в ChromaDB и залогировать результат.

