# Отчёт по заданию 3: создание векторного индекса

## Что сделано

- Реализован скрипт построения индекса: `task_3/build_index.py`.
- Реализован пример поискового запроса к индексу: `task_3/query_example.py`.
- Эмбеддинги получаютcя через `task_3/embedding_client.py`.
- Индекс сохраняется в ChromaDB: `databases/good/chroma_db/`.

## Проверка соответствия заданию

- Выполнены шаги: загрузка документов -> чанкинг -> эмбеддинги -> сохранение в векторную БД.
- В метаданных чанков сохраняются `source`, `title`, `chunk_id`.
- Есть отдельный README с инструкциями запуска: `task_3/README.md`.

## Ключевой вывод

Сформирован рабочий индекс базы знаний и проверен базовый retrieval по пользовательскому запросу.

## Статус

Задание выполнено и готово к сдаче.
